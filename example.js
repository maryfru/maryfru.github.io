// add your code here
console.info("page loaded");
console.debug(2 * 3 + 1);
console.warn(2 * (3 + 1));

var employed = true; // boolean
console.error(employed);

var age = 24; // number

var user = "Mary"; // string
user = user + " Frunzete";

var Skills = [`html`, `css`, `js`];

var dan = {
  age: 26,
  employed: true,
};
var gulia = {
  age: 25,
  employed: true,
};

var jobTitle = document.getElementById("job-title");
